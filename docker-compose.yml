version: '3.8'
services:
  api:
    build: ./api
    ports:
      - "3000:3000"
    env_file:
      - ./api/.env
    environment:
      - NODE_ENV=development
    volumes:
      - ./api:/app
      - /app/node_modules
    restart: unless-stopped
